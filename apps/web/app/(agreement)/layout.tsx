import { redirect } from "next/navigation";
import { AppHeader } from "@/components/app/AppHeader";
import { getCurrentUser } from "@/server/auth/current-user";
export default async function AgreementLayout({ children }: Readonly<{ children: React.ReactNode }>) { const current = await getCurrentUser(); if (current.context.principal.kind === "anonymous") redirect("/sign-in?returnTo=%2Fdashboard"); if (current.context.principal.accountState !== "active") redirect("/account-unavailable"); return <div className="min-h-screen bg-slate-50 text-slate-950"><a href="#main-content" className="fixed left-4 top-2 z-50 -translate-y-20 rounded bg-white px-4 py-2 font-medium shadow focus:translate-y-0">Skip to content</a><AppHeader/><main id="main-content" className="mx-auto max-w-7xl px-4 py-8 sm:px-6 sm:py-10">{children}</main><footer className="mx-auto max-w-7xl border-t px-4 py-6 text-sm text-slate-500 sm:px-6">Sprint 5.4 local development workspace · Disposable sessions · No identity verification, custody, or settlement</footer></div>; }
