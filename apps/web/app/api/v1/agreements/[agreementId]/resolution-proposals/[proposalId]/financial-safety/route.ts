/* eslint-disable @typescript-eslint/no-explicit-any -- transport input is validated by the domain service. */
import { resolutionService } from "../../../../../../../../server/resolution/composition.ts"; import { json, readJson, requestContext, requireAuthenticatedCsrf } from "../../../../../../../../server/agreements/transport/http.ts"; import { mutation, resolutionError } from "../../../../../../../../server/resolution/transport.ts";
export async function POST(request: Request, { params }: { params: Promise<{ agreementId: string; proposalId: string }> }) { const context = await requestContext(request); try { await requireAuthenticatedCsrf(request); const ids = await params; const body = await readJson(request) as any; const result = await resolutionService.safety(context, ids.agreementId, ids.proposalId, body, await mutation(request, body)); return json(result.resource); } catch (error) { return resolutionError(error, context); } }
