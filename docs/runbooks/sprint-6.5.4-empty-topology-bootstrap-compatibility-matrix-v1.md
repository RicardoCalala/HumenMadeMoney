# Sprint 6.5.4 Empty Topology Bootstrap Compatibility Matrix v1

| Artifact / contract | Treatment | Rule |
| --- | --- | --- |
| Sprint 6.5.3 and Sprint 6.4 artifacts | Historical immutable | No edit, import, reinterpretation, or new approval. |
| participant-facing Sprint 6.5.4 v1 artifacts | Historical immutable | No bootstrap input or qualification effect. |
| scorer-calibration Sprint 6.5.4 artifacts | Historical immutable | No fixture, key, response, result, alias, or score is admitted. |
| `custody-topology-v1` | Historical unchanged | Bootstrap evidence is a separate synthetic-only contract, never a real topology approval. |
| `backup-restore-evidence-v1` | Historical unchanged | Bootstrap restore proves only empty/synthetic mechanics, never real RCV eligibility or human separation. |
| `role-custody-ledger-v1` | Historical unchanged | Bootstrap chain is temporary and separate; only a bounded head digest survives. |
| `retention-hold-v1` | Historical unchanged | New approved retention values live in `empty-topology-retention-v1`; no historical bytes are rewritten. |
| `collective-readiness-v2` | Historical unchanged | `collective-readiness-v3` adds bootstrap binding and preserves all v2 blockers. |
| KEY+PRV v2 administration/eligibility | No import | Synthetic controller labels and bootstrap records cannot be remapped, screened, scored, or upgraded. |
| `empty-topology-bootstrap-evidence-v1` | New additive | Only opaque LOC references, control results, zero-effect constants, and digests. |

Any unlisted import, cross-version remap, role/participant/scorer use, or qualification effect fails closed.
