import { json, requestContext } from "../../../../../../../server/agreements/transport/http.ts"; import { evidenceAssessmentService } from "../../../../../../../server/evidence/composition.ts"; import { evidenceError } from "../../../../../../../server/evidence/transport.ts";
export async function GET(request: Request, { params }: { params: Promise<{ agreementId: string; assessmentId: string }> }) { const context = await requestContext(request); try { const p = await params; return json(await evidenceAssessmentService.getAssessment(context, p.agreementId, p.assessmentId)); } catch (error) { return evidenceError(error, context); } }
