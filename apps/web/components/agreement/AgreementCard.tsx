import Link from "next/link";
import { CalendarDays, Users, WalletCards } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { formatDate } from "@/lib/agreements/selectors";
import type { AgreementSummary } from "@/types/agreement";
import { AgreementStatusBadge } from "./AgreementStatusBadge";
export function AgreementCard({ agreement }: { agreement: AgreementSummary }) { return <Card className="h-full transition-shadow hover:shadow-md"><CardHeader><div className="mb-2 flex flex-wrap items-center justify-between gap-2"><AgreementStatusBadge status={agreement.status} /><span className="text-xs text-slate-500">Updated {formatDate(agreement.updatedAt)}</span></div><CardTitle className="text-lg leading-snug"><Link href={`/agreements/${agreement.id}`} className="rounded-sm hover:text-teal-800 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-teal-700">{agreement.title}</Link></CardTitle><p className="line-clamp-2 text-sm leading-6 text-slate-600">{agreement.plainLanguageSummary}</p></CardHeader><CardContent className="grid gap-3 text-sm text-slate-600"><p className="flex items-start gap-2"><Users aria-hidden="true" className="mt-0.5 size-4 shrink-0" />{agreement.participants.map(p => p.displayName).join(" and ")}</p><p className="flex items-start gap-2"><CalendarDays aria-hidden="true" className="mt-0.5 size-4 shrink-0" />Due {formatDate(agreement.deadline)} (UTC)</p><p className="flex items-start gap-2"><WalletCards aria-hidden="true" className="mt-0.5 size-4 shrink-0" />{agreement.funding.mode === "none" ? "No funding protection" : agreement.funding.mode === "protection" ? "Simulated funding protection" : "Conditional payment intent only"}</p><div className="rounded-lg bg-slate-50 p-3"><span className="font-medium text-slate-900">Next action: </span>{agreement.nextAction}</div></CardContent></Card>; }
