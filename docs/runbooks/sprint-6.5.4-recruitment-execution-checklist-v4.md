# Sprint 6.5.4 Recruitment Execution Checklist v4

Version: `sprint-6.5.4-recruitment-execution-checklist-v4`

Status: **PENDING — BLOCKED — NO REAL KEY+PRV SCREENING — NO REAL SCORER CALIBRATION — NO A-01 CONTACT**

This checklist prospectively supersedes v3 only after explicit founder approval of the new randomized-order implementation checkpoint. Checklist v3 and all older records remain immutable history.

## Randomized KEY+PRV implementation checkpoint

- [ ] Exact implementation commit, governance HEAD, expanded amendment envelope and envelope SHA-256 match.
- [ ] Founder approval receipt explicitly names the new pending randomized-order checkpoint; design approval is not reused.
- [ ] Exact v2 protocol, instrument manifest, administration/eligibility/assignment/readiness schemas, evaluator, tests and compatibility matrix match the envelope.
- [ ] All 24 IDs and statement bytes match frozen v1 Section 2; no wording or substantive eligibility change.
- [ ] One locked mixed permutation uses the approved OS-CSPRNG/HMAC/rejection-sampled Fisher-Yates method and passes deterministic replay/digest verification.
- [ ] Atomic private uniqueness and durable lock exist before presentation; reroll, silent restart, substitution and out-of-order capture fail closed.
- [ ] Abort, resume, incident, correction and replacement evidence are linked and append only; prior responses are never copied.
- [ ] `fixed_accessibility` is pre-lock, canonical KEY then PRV, seedless, neutral-code-only and eligibility-neutral.
- [ ] Keyboard, screen-reader, magnification, switch-control, focus, ordinal, pause/resume and plainly named True/False operation are verified.
- [ ] Physical selection assistance repeats exact wording only, never explains or paraphrases; ambiguity invokes repeat/stop and is never inferred as True.
- [ ] Compatibility report proves byte-identical reuse of named v1 custody/recovery/ledger/retention contracts and rejects every unlisted cross-version mix.
- [ ] Private evidence location, access list, retention period and audit authority for future real evidence are explicitly approved outside Git.

## Existing readiness and pre-contact gates

- [ ] Every unchanged checklist-v3 role, separation, custody, recovery, retention, incident, scorer and pre-contact control remains current and passing.
- [ ] `collective-readiness-v2` has a valid founder receipt and every v2 control passes before real KEY+PRV screening.
- [ ] Real scorer calibration remains separately blocked until the applicable complete role/custody gate authorizes it.
- [ ] `A-01` remains blocked until two distinct real scorers pass and every versioned pre-contact gate is current.
- [ ] Counters remain `0/16` contacted, `0/12` enrolled, A/B/C/D `0/3` each, release reviewers `0/2`, genuine reviews `0/30`, HMM `NOT_QUALIFIED`.

Any missing, false, stale, ambiguous, incident-affected, mismatched, cross-version, unauthorized or unverifiable item stops execution. No founder/operator override, partial score, threshold, waiver, coaching or favorable subset is allowed.
