import { json, requestContext } from "../../../../../../../server/agreements/transport/http.ts"; import { evidenceAssessmentService } from "../../../../../../../server/evidence/composition.ts"; import { evidenceError } from "../../../../../../../server/evidence/transport.ts";
export async function GET(request: Request, { params }: { params: Promise<{ agreementId: string; evidenceId: string }> }) { const context = await requestContext(request); try { const { agreementId, evidenceId } = await params; return json(await evidenceAssessmentService.getEvidence(context, agreementId, evidenceId)); } catch (error) { return evidenceError(error, context); } }
