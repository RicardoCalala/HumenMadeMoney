# Sprint 6.5.4 Recruitment Execution Checklist v5

Status: **PENDING — BLOCKED — EMPTY BOOTSTRAP EVIDENCE ONLY — NO REAL KEY+PRV SCREENING — NO REAL SCORER CALIBRATION — NO A-01 CONTACT**

This prospective checklist supersedes v4 only after explicit approval of the new bootstrap implementation checkpoint. v4 and every older checklist/checkpoint remain immutable history.

## Empty topology bootstrap

- [ ] Exact implementation commit, governance HEAD, evidence digest, expanded envelope, and checkpoint digest match.
- [ ] Eight unique opaque LOC references exist for IDMAP, RAW, LEDGER, CAL, PRES, KEYS, BACKUP, and INC; actual paths remain outside Git.
- [ ] Root is inside the authorized project, ignored before creation, restricted, and has no off-limits overlap.
- [ ] Primary and BACKUP control boundaries are distinct and every final compartment is empty with mode 0700.
- [ ] Deny-by-default synthetic non-person controller templates had zero grants and were deleted after evidence capture.
- [ ] AES-256-GCM probes pass with distinct keys and IDMAP/KEYS/BACKUP separation.
- [ ] XOR 2-of-2 recovery passes; either share alone fails; no secret persists; all synthetic secret material is destroyed.
- [ ] Operational encrypted BACKUP and isolated non-overwrite restore match manifest, count, head, and permissions and are cleaned.
- [ ] Synthetic ledger/correction/incident chain passes, mutation fails, independent head is recorded, and records are removed.
- [ ] Retention/hold values, 168-hour backup deletion maximum, distinct real deletion verification, and immediate bootstrap cleanup match.
- [ ] Bootstrap authority is expired and real PRV/RCV handoff remains not started.
- [ ] Strict schemas, deterministic evaluation, history digests, privacy/secret scans, Git guards, full repository checks, and zero-network production-shaped build pass.

## Collective gate

- [ ] Founder approval receipt explicitly names the new pending bootstrap checkpoint; design approvals are not reused.
- [ ] Randomized-order checkpoint is independently approved and current.
- [ ] Real PRV and RCV independently qualify and accept the topology with newly established operational credentials/factors.
- [ ] A real human-separated operational restore passes without topology mismatch.
- [ ] Every unchanged v1/v2 role, separation, calibration, custody, incident, and pre-contact control passes.

Until all boxes are truthfully complete, `collective-readiness-v3` is `BLOCKED`, real KEY+PRV screening and scorer calibration are unauthorized, A-01 contact is false, counters remain 0/16 contacted, 0/12 enrolled, A/B/C/D 0/3, release reviewers 0/2, genuine reviews 0/30, and HMM is `NOT_QUALIFIED`.
