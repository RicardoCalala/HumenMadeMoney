import { AlertCircle, Clock3, LockKeyhole } from "lucide-react";
export function DataStateNotice({state}:{state:"stale"|"unavailable"|"permission_denied"}) { const content={stale:{Icon:Clock3,title:"This information may be out of date",text:"Keep the current view for reference, then refresh before making a decision."},unavailable:{Icon:AlertCircle,title:"Agreement information is unavailable",text:"Nothing changed. Try again when the data source is available."},permission_denied:{Icon:LockKeyhole,title:"You do not have permission to view this section",text:"Other agreement details remain available. Ask an authorized participant if access is expected."}}[state]; return <div role="status" className="flex gap-3 rounded-xl border bg-white p-4"><content.Icon aria-hidden="true" className="mt-0.5 size-5 shrink-0"/><div><h3 className="font-semibold">{content.title}</h3><p className="mt-1 text-sm text-slate-600">{content.text}</p></div></div> }
