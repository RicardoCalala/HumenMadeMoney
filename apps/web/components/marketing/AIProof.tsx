import { Bot, Eye, Scale } from "lucide-react";

export function AIProof() {
  const points = [[Bot, "Evaluation before enablement", "Offline synthetic tests measure advisory assessment quality; provider-backed AI is disabled by default."], [Eye, "A shared source of truth", "Every party sees the same milestones, evidence, and agreement history."], [Scale, "Rules that stay readable", "Plain-language terms make the path forward understandable when details matter."]] as const;
  return <section className="border-y border-white/10 bg-slate-900/50"><div className="mx-auto grid max-w-6xl gap-12 px-6 py-24 lg:grid-cols-[1fr_1.15fr] lg:px-8"><div><p className="text-sm font-medium text-teal-300">Advisory AI in development</p><h2 className="mt-3 text-3xl font-semibold tracking-tight text-white sm:text-4xl">Measured guidance with human authority.</h2><p className="mt-5 max-w-md leading-7 text-slate-400">The current product uses deterministic assessment and human review. Provider-backed AI is a gated development integration, not a generally enabled capability.</p></div><div className="space-y-5">{points.map(([Icon, title, body]) => <div key={title} className="flex gap-4 rounded-xl border border-white/10 bg-slate-950/50 p-5"><Icon className="mt-0.5 size-5 shrink-0 text-teal-300" /><div><h3 className="font-medium text-white">{title}</h3><p className="mt-1 leading-6 text-slate-400">{body}</p></div></div>)}</div></div></section>;
}
