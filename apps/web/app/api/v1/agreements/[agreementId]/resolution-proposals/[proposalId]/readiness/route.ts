import { resolutionService } from "../../../../../../../../server/resolution/composition.ts"; import { json, requestContext } from "../../../../../../../../server/agreements/transport/http.ts"; import { resolutionError } from "../../../../../../../../server/resolution/transport.ts";
export async function GET(request: Request, { params }: { params: Promise<{ agreementId: string; proposalId: string }> }) { const context = await requestContext(request); try { const { agreementId, proposalId } = await params; return json(await resolutionService.evaluate(context, agreementId, proposalId)); } catch (error) { return resolutionError(error, context); } }
